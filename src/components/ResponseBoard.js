import React from "react";

const ResponseBoard = ({ response }) => {
  return (
    <div className="response-container" style={{ width: "50%" }}>
      <h2>AI Assistant Response</h2>
    </div>
  );
};

export default ResponseBoard;
